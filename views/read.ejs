<!DOCTYPE html>

<html>

<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
</head>

<style>
    /* .row {
        border: 1px solid red;
    }

    div[class^="col-"] {
        border: 1px solid green;
    } */
</style>

<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-12 col-sm-8 mx-auto bg-light border rounded">

                <div class="row">

                    <div class="col-12 mt-2">

                        <h3>Posts:</h3>
                        <div class="col-12 mt-2 mb-2 bg-white text-dark border rounded">
                            <p class="font-weight-normal" id="get-title-result"></p>
                            <p class="font-weight-light" id="get-content-result"></p>  
                        </div>   
                    </div>

                </div>


                <div class="row">
                    <div class="col-12 col-sm-8 mb-2 mt-2 text-left">
                        <h7 id="post-confirmation"></h7>
                    </div>

                    <div class="col-12 col-sm-4 mb-2 mt-2">
                        <a href="http://localhost:3000/articles/write" class="btn btn-link btn-sm btn-block">back</a>
                        <!-- post() -->
                    </div>

                </div>
            </div>
        </div>
    </div>
</body>

<script>





    function get() {
        axios.get("/articles/")
            .then(function (response) {
                console.log(response.data)

                for (var i = 0; i < response.data.length; i++) {
                    console.log(response.data[i].postTitle);
                    console.log(response.data[i].postContent);

                    var title = "<br><br><h6>" + "Title: " + response.data[i].postTitle + "</h6>";
                    var content = "<p>" + response.data[i].postContent + "</p><hr>";

                    var result = title + content;

                    console.log(result);

                    document.getElementById("get-title-result").innerHTML += result;
                }
            })
            .catch(function (error) {
                console.log(error);
            });
    };

    get();

    // document.getElementById("get-title-result").innerHTML = JSON.stringify(


    // document.getElementById("get-title-result").innerHTML = JSON.stringify(response.data[0].postTitle + "<br>" + response.data[0].postContent); 


    // function get() {
    //     var url = document.getElementById("get-input").value;

    //     axios.get(url)
    //         .then(function (response) {
    //             document.getElementById("get-content-result").innerHTML = JSON.stringify(response.data);
    //         })
    //         .catch(function (error) {
    //             console.log(error);
    //         });
    // };

</script>

</html>